<div class="container">
  <% include alert %>
  <% if (!user) { %>
  <!-- 巨幕欢迎墙 -->
    <div class="jumbotron">
      <h1>Hello, world!</h1>
      <p>这是一个基于NodeJs的微博系统</p>
      <p>
        <a class="btn btn-primary btn-lg" href="/login" role="button">登录</a>
        <a class="btn btn-lg" href="/reg" role="button">注册</a>
      </p>
    </div>
  <% } %>
  <% if (user) { %>
    <% include say %>
    <% } %>
    <% include posts2 %>
</div>
